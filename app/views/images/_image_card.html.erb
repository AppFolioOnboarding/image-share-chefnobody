<div class="js-image-card">
    <img src="<%= image.url %>" />
    <%= render partial: "tag_list", locals: { tag_list: image.tag_list, image_id: image.id } %>
    <%= button_to "Delete", image_path(image), method: :delete, data: { confirm: "Are you sure?" } %>
</div>